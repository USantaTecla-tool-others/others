<!--  
var TargetView = function(target, meshView) {
	this.target = target;
	this.targetColor = '#0FF';
	this.meshView = meshView;
};

TargetView.prototype = {
	draw: function(target) {
		this.meshView.drawCell(target, this.targetColor);
	}
};

//-->